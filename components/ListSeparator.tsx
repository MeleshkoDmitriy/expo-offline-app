import { StyleSheet, View } from "react-native";

export const ListSeparator = () => <View style={styles.container} />;

const styles = StyleSheet.create({
  container: {
    borderWidth: 1,
  },
});
